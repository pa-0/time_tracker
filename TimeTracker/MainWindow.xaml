<Window x:Class="Ficksworkshop.TimeTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:Ficksworkshop.TimeTracker"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <TabControl HorizontalAlignment="Left" Height="299" Margin="7,10,0,0" VerticalAlignment="Top" Width="500">
            <TabItem Header="Projects">
                <StackPanel>
                    <DataGrid ItemsSource="{Binding Projects}" SelectedItem="{Binding SelectedProject}" AutoGenerateColumns="False" IsReadOnly="False" VerticalAlignment="Top" Height="219" Width="476">
                        <DataGrid.Columns>
                            <DataGridTextColumn Binding="{Binding Path=UniqueId, Mode=TwoWay}" Header="Id"/>
                            <DataGridTextColumn Binding="{Binding Path=Name, Mode=TwoWay}" Header="Name"/>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Orientation="Horizontal">
                        <Button Content="New Project" Height="22" Width="75" Command="{x:Static local:Commands.CreateNewProjectCommand}" CommandParameter="{Binding DataSet}"/>
                        <Button Content="Delete Project" Height="22" Width="75" Command="{x:Static local:Commands.DeleteProjectCommand}" CommandParameter="{Binding SelectedProject}"/>
                        <Button Content="Punch In/Out" Height="22" Width="75" Command="{x:Static local:Commands.PunchInOutCommand}" CommandParameter="{Binding SelectedProject}"/>
                    </StackPanel>
                </StackPanel>
            </TabItem>
            <TabItem Header="Settings">
                <DataGrid ItemsSource="{Binding Settings}" AutoGenerateColumns="False" Margin="10,10,0,0" VerticalAlignment="Top" Height="200" Width="300">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Path=Name, Mode=OneWay}" Header="Setting"/>
                        <DataGridTextColumn Binding="{Binding Path=Value, Mode=TwoWay}" Header="Value"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
