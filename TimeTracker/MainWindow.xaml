<Window x:Class="Ficksworkshop.TimeTracker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:timeTrackerApi="clr-namespace:Ficksworkshop.TimeTrackerAPI;assembly=TimeTrackerAPI"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <DataGrid Name="SettingsGrid" AutoGenerateColumns="False" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" Height="115" Width="497">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path=Name, Mode=OneWay}" Header="Setting"/>
                <DataGridTextColumn Binding="{Binding Path=Value, Mode=TwoWay}" Header="Value"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
